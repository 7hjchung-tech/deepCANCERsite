# Install with:  python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
pandas
numpy
torch
transformers
scipy
scikit-learn
requests

# data/structure/code/ (struct feature pipeline) — no prebuilt wheel for very
# new Python versions (needs the biotraj dependency's compiled extension)
biotite
