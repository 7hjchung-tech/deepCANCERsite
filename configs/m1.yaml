# M1: vanilla (frozen) ESM encoder, Block B used, cached diff_emb.
# Differs from M2 in use_block_b ONLY, and from M3 in esm_mode/use_lora ONLY.
# Optimisation settings live in base.yaml on purpose — see the note there.
extends: base.yaml
model_id: M1

esm_mode: cached
use_lora: false
use_block_b: true
