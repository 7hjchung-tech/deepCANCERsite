# M2: vanilla (frozen) ESM encoder, Block B NOT used, cached diff_emb.
# Differs from M1 in use_block_b ONLY, and from M4 in esm_mode/use_lora ONLY.
# Optimisation settings live in base.yaml on purpose — see the note there.
extends: base.yaml
model_id: M2

esm_mode: cached
use_lora: false
use_block_b: false
